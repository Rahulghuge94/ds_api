model = "qwen3.7-plus"
model_provider = "qwen"

[model_providers.qwen]
name = "Qwen Custom Endpoint"
base_url = "http://localhost:8000/qwen/v1"
# if you have set the env variable then turn on or use direct header below
# env_key = "x-api-key"

[headers]
# Some clients may support custom headers via config. If so, include:
# x-api-key = "your-secret-key"
